language: node_js
node_js: 10
script: npm run ci
after_success: |
  if [ "${TRAVIS_BRANCH}" == "master" ]; then
    npm run coverage
    curl -X POST --data-urlencode "gitUrl=git@github.com:ViBiOh/funtch.git" https://doc.esdoc.org/api/create
  fi
notifications:
  email: false
